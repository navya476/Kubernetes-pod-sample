apiVersion: "v1"
kind: Pod
metadata:
  name: javaweb
  labels:
    name: javaweb
    app: java
spec:
  containers:
    - name: javaweb
      image: navyateja/java-app
      ports:
        - containerPort: 8080
          name: http
          protocol: TCP

